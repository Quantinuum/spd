# README Internal Notes

These are points removed from the root `README.md` during the README cleanup. Keep them here in case we want to restore or reuse them later.

- The old `Architecture` section with the pipeline line `frontend input -> frontend parser -> internal circuit IR -> backend adapter -> backend kernels -> SPO/SPGO`
- The long explanation of supported `init_gradient_spo(...)` loss options
- The OpenQASM compatibility warning about `pytket.qasm` canonicalizing or reordering gates
- The detailed `max_num_str` behavior note, including the JAX power-of-two rounding detail
- The `Near-Term Extensions` section
